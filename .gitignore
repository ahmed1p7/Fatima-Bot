plugins/menu.mjs has been modified, but it's a source file (.mjs). No build artifacts, dependencies, or temporary files are present in the changes. Therefore, no additional ignore rules are needed.

(empty)